# Two-document YAML stream for multi-doc parser tests.
# Original — written for dq-core.
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  log_level: info
---
apiVersion: v1
kind: Service
metadata:
  name: app
spec:
  ports:
    - port: 80
      targetPort: 8080
