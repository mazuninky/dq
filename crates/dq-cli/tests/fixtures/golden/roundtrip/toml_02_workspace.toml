[workspace]
members = [
    "crates/foo",
    "crates/bar",
]
resolver = "2"

[workspace.package]
edition = "2021"
license = "MIT OR Apache-2.0"
authors = ["Example <example@example.test>"]

[workspace.dependencies]
clap = { version = "4", features = ["derive"] }
serde = "1"
