# Helm values fixture for dq-cli integration tests.
# Original — not copied from any chart.
image:
  repository: registry.example.test/svc
  tag: stable
  pullPolicy: IfNotPresent
service:
  type: ClusterIP
  port: 80
resources:
  limits:
    cpu: "500m"
    memory: 256Mi
  requests:
    cpu: "100m"
    memory: 64Mi
nodeSelector:
  role: worker
