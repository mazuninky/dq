tests:
  - name: well-formed evals.json passes
    input: |
      {
        "skill_name": "my-skill",
        "evals": [
          {
            "id": 1,
            "prompt": "do the thing",
            "expected_output": "Uses tool foo ...",
            "assertions": [
              { "text": "uses -F toon", "type": "output" }
            ]
          }
        ]
      }
    format: json
    expected:
      violations: []

  - name: missing expected_output fires
    input: |
      {
        "skill_name": "my-skill",
        "evals": [
          {
            "id": 1,
            "prompt": "x",
            "assertions": [
              { "text": "y", "type": "output" }
            ]
          }
        ]
      }
    format: json
    expected:
      violations:
        - rule: skills.evals-schema
          message_contains: "expected_output"

  - name: empty assertions array fires
    input: |
      {
        "skill_name": "my-skill",
        "evals": [
          {
            "id": 1,
            "prompt": "x",
            "expected_output": "y",
            "assertions": []
          }
        ]
      }
    format: json
    expected:
      violations:
        - rule: skills.evals-schema
          message_contains: "less than 1 item"

  - name: bogus assertion type fires
    input: |
      {
        "skill_name": "my-skill",
        "evals": [
          {
            "id": 1,
            "prompt": "x",
            "expected_output": "y",
            "assertions": [
              { "text": "z", "type": "wat" }
            ]
          }
        ]
      }
    format: json
    expected:
      violations:
        - rule: skills.evals-schema

  - name: unrelated json file does not fire
    input: |
      { "name": "package", "version": "1.0.0" }
    format: json
    expected:
      violations: []
